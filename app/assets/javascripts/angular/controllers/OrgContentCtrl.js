'use strict';

app.controller('OrgContentCtrl', ['$scope', '$routeParams', '$location', '$rootScope', function($scope, $routeParams, $location, $rootScope) {

  // console.log($location.$$path)

  // $rootScope.orgId = $routeParams.id

  // console.log($rootScope.orgId)

}]);
